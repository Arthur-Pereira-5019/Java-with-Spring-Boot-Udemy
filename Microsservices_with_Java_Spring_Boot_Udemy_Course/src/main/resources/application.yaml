spring:
  application:
    name: integration_tests
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/udemy4?useTimezone=true&serverTimezone=UTC
    username: root
    password: Art.DB25
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
    show-sql: true
    open-in-view: true
logging:
  level:
    root: Info
spring-doc:
  paths-to-match: /**/v1/**
  swagger-ui:
    use-root-path: true
cors:
  originPatterns: http://localhost:8080,http://localhost:3000